[project]
name = "capstone-ai-engineering-buildcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "chromadb>=1.5.9",
    "jupyter>=1.1.1",
    "openai>=2.40.0",
    "pdfplumber>=0.11.9",
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "sentence-transformers>=5.5.1",
    "sqlite-utils>=3.39",
    "streamlit>=1.58.0",
]

[tool.pytest.ini_options]
markers = [
    "slow: real model / real PDF — slow, skip with -m \"not slow\"",
]
